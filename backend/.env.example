PGHOST=localhost
PGPORT=5432
PGDATABASE=dhammahadaya
PGUSER=dhammahadaya_app
PGPASSWORD=

PORT=4000
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
SESSION_SECRET=
# local | r2 (r2 not implemented yet — see backend/src/storage/)
STORAGE_DRIVER=local
# console | smtp (smtp not implemented yet — see backend/src/email/)
EMAIL_DRIVER=console
